package main

import "fmt"

func main() {
    var numero, soma, quantidade int
    fmt.Println("Digite os números inteiros (digite 0 para encerrar):")

    for {
        fmt.Print("Número: ")
        fmt.Scanln(&numero)

        if numero == 0 {
            break
        }

        soma += numero
        quantidade++
    }

    if
